# No backend secrets required.
# This is a zero-backend, browser-only application.
# GitHub Personal Access Tokens are entered at runtime via the Login page
# and stored only in browser memory + sessionStorage.

# Optional: if you ever add a local mock server or CI environment variables,
# they would go here. For now, this file is intentionally minimal.
